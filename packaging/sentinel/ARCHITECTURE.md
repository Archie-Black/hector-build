# Eternal Sentinel

Local roommate. Hector is the mouth. Alpha / Beta / Gamma are silent.

- **Alpha (Nexus):** routes intent, glues thoughts, edge jobs.
- **Beta (Tactician):** long or hard jobs (code, strategy).
- **Gamma (Subconscious):** dreams when you stop talking. Consolidates memory, prunes the graph, mutates delay/retrieval mix if KPIs slip.

Memory does not leave the machine. Embeddings are a 32-d braid hash, not a cloud model. The graph is in-process. Ollama/vLLM is optional, on the private network only.

Charter, bond, and vault laws do not mutate.
