# Spectral HX Aether sits on PipeWire. Stereo out. Surround if the box has it.
context.properties = {
  default.clock.rate = 48000
  default.clock.quantum = 256
  default.clock.min-quantum = 64
  default.clock.max-quantum = 1024
}
