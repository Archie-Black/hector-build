#!/usr/bin/env bash
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
if [ "${HECTOR_HOOKS:-1}" = "0" ]; then
  exit 0
fi
git diff --cached --name-only --diff-filter=ACM | node scripts/hook-scan.mjs
